{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yomineko.dev/contracts/stroke_kana.schema.json",
  "title": "stroke_kana",
  "description": "Stroke data for one kana character, including the shadow guides used when tracing.",
  "x-yomineko": {
    "entity": "stroke_kana",
    "records": 162,
    "packing": "list",
    "glob": "corpus/strokes/kana.json",
    "stable_id_field": null,
    "generated_by": "scripts/contracts/build_schemas.py from contracts/_shapes.json",
    "vocabulary_policy": "Types, nesting and `required` are measured from the data. Value sets are not: every `enum` here is either declared in a design document, parsed from the code that produces the value, or curated in build_schemas.py with its source named in x-vocabulary. See contracts/README.md."
  },
  "type": "object",
  "properties": {
    "char": {
      "type": "string"
    },
    "kind": {
      "type": "string",
      "enum": [
        "hiragana",
        "katakana"
      ],
      "description": "Which script this glyph belongs to. Vocabulary owner: curated — the two kana scripts. Widening it is an edit there, never a side effect of regenerating this schema from the data.",
      "x-vocabulary": {
        "owner": "curated",
        "source": "the two kana scripts"
      }
    },
    "license": {
      "type": "string",
      "minLength": 1,
      "description": "The upstream dataset's licence identifier (see ATTRIBUTION.md). Free text: a new dataset brings a new licence, which is not a contract violation."
    },
    "shadows": {
      "type": "array",
      "items": {
        "type": "array"
      }
    },
    "shadows[]": {
      "type": "object",
      "items": {
        "type": "string"
      }
    },
    "source": {
      "$ref": "common.schema.json#/$defs/Provenance/properties/source"
    },
    "strokes": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "viewbox": {
      "type": "string"
    }
  },
  "additionalProperties": false,
  "required": [
    "char",
    "kind",
    "license",
    "shadows",
    "source",
    "strokes",
    "viewbox"
  ]
}
