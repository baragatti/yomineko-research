{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yomineko.dev/contracts/grammar.schema.json",
  "title": "grammar",
  "description": "One grammar point: its forms, how it is formed, what it contrasts with, and the pedagogy around it.",
  "x-yomineko": {
    "entity": "grammar",
    "records": 494,
    "packing": "list",
    "glob": "corpus/grammar/*.json",
    "stable_id_field": "slug",
    "generated_by": "scripts/contracts/build_schemas.py from contracts/_shapes.json",
    "vocabulary_policy": "Types, nesting and `required` are measured from the data. Value sets are not: every `enum` here is either declared in a design document, parsed from the code that produces the value, or curated in build_schemas.py with its source named in x-vocabulary. See contracts/README.md."
  },
  "type": "object",
  "properties": {
    "caution": {
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "none",
            "offensive",
            "rough",
            "sensitive"
          ],
          "description": "How careful a learner has to be with this pattern. Vocabulary owner: design — design/schema_v2.md ('Grammar': caution). Widening it is an edit there, never a side effect of regenerating this schema from the data.",
          "x-vocabulary": {
            "owner": "design",
            "source": "design/schema_v2.md ('Grammar': caution)"
          }
        },
        {
          "type": "null"
        }
      ]
    },
    "explanation": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "families": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "formation": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "formation_steps": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "variants": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "base": {
                "type": "string",
                "enum": [
                  "any",
                  "clause",
                  "i-adjective",
                  "na-adjective",
                  "noun",
                  "verb"
                ],
                "description": "What the formation step attaches to. Vocabulary owner: producer — scripts/validate/validate_grammar_formation.py BASES. Widening it is an edit there, never a side effect of regenerating this schema from the data.",
                "x-vocabulary": {
                  "owner": "producer",
                  "source": "scripts/validate/validate_grammar_formation.py BASES"
                }
              },
              "example": {
                "type": "string"
              },
              "steps": {
                "type": "array"
              }
            }
          }
        }
      },
      "additionalProperties": false
    },
    "forms": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "form": {
            "type": "string"
          },
          "meaning": {
            "anyOf": [
              {
                "$ref": "common.schema.json#/$defs/LocaleText"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "id": {
      "type": "integer",
      "minimum": 0
    },
    "key": {
      "type": "string"
    },
    "label": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "level": {
      "$ref": "common.schema.json#/$defs/Level"
    },
    "level_agreement": {
      "$ref": "common.schema.json#/$defs/LevelTag/properties/level_agreement"
    },
    "level_confidence": {
      "$ref": "common.schema.json#/$defs/LevelTag/properties/level_confidence"
    },
    "level_sources": {
      "$ref": "common.schema.json#/$defs/LevelTag/properties/level_sources"
    },
    "needs_review": {
      "$ref": "common.schema.json#/$defs/Provenance/properties/needs_review"
    },
    "nuance": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "nuance_tags": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Free semantic tags on a grammar point. No document defines the tag set, so the contract does not pretend one is closed — an authored tag nobody declared should be caught by review, not by an enum measured from the corpus."
      }
    },
    "refs": {
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "also_known_as": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "label_en": {
          "type": "string"
        },
        "level_sources": {
          "$ref": "common.schema.json#/$defs/LevelTag/properties/level_sources"
        }
      },
      "additionalProperties": false
    },
    "register": {
      "type": "array",
      "items": {
        "type": "string",
        "enum": [
          "casual",
          "colloquial",
          "dated",
          "feminine",
          "formal",
          "honorific",
          "humble",
          "literary",
          "masculine",
          "neutral",
          "plain",
          "polite",
          "slang",
          "written"
        ],
        "description": "Registers this grammar point belongs to. Vocabulary owner: design — design/schema_v2.md ('Grammar': the register array) + the grammar_point table comment. Widening it is an edit there, never a side effect of regenerating this schema from the data.",
        "x-vocabulary": {
          "owner": "design",
          "source": "design/schema_v2.md ('Grammar': the register array) + the grammar_point table comment"
        }
      }
    },
    "related": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Keys of related grammar points. A reference list, not a vocabulary."
      }
    },
    "review_status": {
      "type": "array",
      "description": "Human review verdicts that are still LIVE for this record: each one's content hash still matches the text it approved. Written by the exporter from research/derived/review_ledger.json, never by hand — an entry whose text was rewritten afterwards is stale and exports nothing, and a review_status the ledger does not justify is a hard failure (scripts/validate/validate_review_ledger.py). Absent on a record nobody has reviewed, which is why it is never required. See design/review_ledger.md.",
      "items": {
        "type": "object",
        "properties": {
          "field": {
            "type": "string",
            "minLength": 1,
            "description": "The reviewed field, or \"*\" for the whole record."
          },
          "locale": {
            "anyOf": [
              {
                "$ref": "common.schema.json#/$defs/Locale"
              },
              {
                "type": "null"
              }
            ],
            "description": "The locale reviewed; null for a locale-agnostic verdict."
          },
          "status": {
            "type": "string",
            "enum": [
              "approved",
              "rejected"
            ],
            "description": "The verdict a named reviewer recorded. Vocabulary owner: design — design/review_ledger.md. Widening it is an edit there, never a side effect of regenerating this schema from the data.",
            "x-vocabulary": {
              "owner": "design",
              "source": "design/review_ledger.md"
            }
          },
          "reviewed_by": {
            "type": "string",
            "minLength": 1,
            "description": "Who reviewed it. An anonymous approval is not a review, and the correction-rate metric is per person."
          },
          "approved_at": {
            "type": "string",
            "minLength": 1,
            "description": "When. Provenance and ordering only — approvals do not expire (APP_PLAN D4): a rewrite invalidates them, the calendar does not."
          },
          "content_hash": {
            "type": "string",
            "pattern": "^(sha256:)?[0-9a-fA-F]{8,64}$",
            "description": "The sha256 of exactly what was reviewed, so a reader can tell WHICH text was approved without the ledger."
          }
        },
        "required": [
          "field",
          "status",
          "reviewed_by",
          "approved_at",
          "content_hash"
        ],
        "additionalProperties": false
      }
    },
    "slug": {
      "$ref": "common.schema.json#/$defs/StableId"
    },
    "steps_unavailable": {
      "type": [
        "string",
        "null"
      ]
    },
    "structure_pattern": {
      "type": "string"
    },
    "usage_contexts": {
      "type": "array",
      "items": {
        "type": "string",
        "description": "Situations this pattern belongs to. No declared taxonomy."
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "caution",
    "explanation",
    "families",
    "formation",
    "formation_steps",
    "forms",
    "id",
    "key",
    "label",
    "level",
    "level_agreement",
    "level_confidence",
    "level_sources",
    "needs_review",
    "nuance",
    "nuance_tags",
    "refs",
    "register",
    "related",
    "slug",
    "steps_unavailable",
    "structure_pattern",
    "usage_contexts"
  ]
}
