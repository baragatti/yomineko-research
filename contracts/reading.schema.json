{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yomineko.dev/contracts/reading.schema.json",
  "title": "reading",
  "description": "A short reading passage gated to a lesson, with its tokens and translation.",
  "x-yomineko": {
    "entity": "reading",
    "records": 286,
    "packing": "list",
    "glob": "corpus/readings/*.json",
    "stable_id_field": "slug",
    "generated_by": "scripts/contracts/build_schemas.py from contracts/_shapes.json",
    "vocabulary_policy": "Types, nesting and `required` are measured from the data. Value sets are not: every `enum` here is either declared in a design document, parsed from the code that produces the value, or curated in build_schemas.py with its source named in x-vocabulary. See contracts/README.md."
  },
  "type": "object",
  "properties": {
    "ai_generated": {
      "$ref": "common.schema.json#/$defs/Provenance/properties/ai_generated"
    },
    "gated_to_lesson": {
      "type": "string"
    },
    "jp": {
      "type": "string"
    },
    "layer": {
      "$ref": "common.schema.json#/$defs/Layer"
    },
    "length_band": {
      "type": "string",
      "enum": [
        "long",
        "paragraph",
        "short"
      ],
      "description": "Roughly how long the passage is. Vocabulary owner: design — design/reading_practice.md (length_band). Widening it is an edit there, never a side effect of regenerating this schema from the data.",
      "x-vocabulary": {
        "owner": "design",
        "source": "design/reading_practice.md (length_band)"
      }
    },
    "level": {
      "$ref": "common.schema.json#/$defs/Level"
    },
    "needs_review": {
      "$ref": "common.schema.json#/$defs/Provenance/properties/needs_review"
    },
    "review_status": {
      "type": "array",
      "description": "Human review verdicts that are still LIVE for this record: each one's content hash still matches the text it approved. Written by the exporter from research/derived/review_ledger.json, never by hand — an entry whose text was rewritten afterwards is stale and exports nothing, and a review_status the ledger does not justify is a hard failure (scripts/validate/validate_review_ledger.py). Absent on a record nobody has reviewed, which is why it is never required. See design/review_ledger.md.",
      "items": {
        "type": "object",
        "properties": {
          "field": {
            "type": "string",
            "minLength": 1,
            "description": "The reviewed field, or \"*\" for the whole record."
          },
          "locale": {
            "anyOf": [
              {
                "$ref": "common.schema.json#/$defs/Locale"
              },
              {
                "type": "null"
              }
            ],
            "description": "The locale reviewed; null for a locale-agnostic verdict."
          },
          "status": {
            "type": "string",
            "enum": [
              "approved",
              "rejected"
            ],
            "description": "The verdict a named reviewer recorded. Vocabulary owner: design — design/review_ledger.md. Widening it is an edit there, never a side effect of regenerating this schema from the data.",
            "x-vocabulary": {
              "owner": "design",
              "source": "design/review_ledger.md"
            }
          },
          "reviewed_by": {
            "type": "string",
            "minLength": 1,
            "description": "Who reviewed it. An anonymous approval is not a review, and the correction-rate metric is per person."
          },
          "approved_at": {
            "type": "string",
            "minLength": 1,
            "description": "When. Provenance and ordering only — approvals do not expire (APP_PLAN D4): a rewrite invalidates them, the calendar does not."
          },
          "content_hash": {
            "type": "string",
            "pattern": "^(sha256:)?[0-9a-fA-F]{8,64}$",
            "description": "The sha256 of exactly what was reviewed, so a reader can tell WHICH text was approved without the ledger."
          }
        },
        "required": [
          "field",
          "status",
          "reviewed_by",
          "approved_at",
          "content_hash"
        ],
        "additionalProperties": false
      }
    },
    "slug": {
      "$ref": "common.schema.json#/$defs/StableId"
    },
    "source_slugs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "title": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "tokens": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "pos": {
            "type": "string",
            "enum": [
              "adnominal",
              "adverb",
              "auxiliary",
              "conjunction",
              "filler",
              "i-adjective",
              "interjection",
              "na-adjective",
              "noun",
              "numeral",
              "particle",
              "prefix",
              "pronoun",
              "punctuation",
              "suffix",
              "symbol",
              "verb",
              "whitespace"
            ],
            "description": "Part of speech, Layer A, mechanical from the analyzer. Vocabulary owner: producer — scripts/ingest/dissect.py POS_MAP (Sudachi/UniDic 品詞 → neutral enum). Widening it is an edit there, never a side effect of regenerating this schema from the data.",
            "x-vocabulary": {
              "owner": "producer",
              "source": "scripts/ingest/dissect.py POS_MAP (Sudachi/UniDic 品詞 → neutral enum)"
            }
          },
          "r": {
            "type": "string"
          },
          "ro": {
            "type": "string"
          },
          "s": {
            "type": "string"
          }
        },
        "additionalProperties": false
      }
    },
    "translation": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "uses": {
      "type": "object",
      "properties": {
        "kanji": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "vocab": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      },
      "required": [
        "kanji",
        "vocab"
      ],
      "additionalProperties": false
    }
  },
  "additionalProperties": false,
  "required": [
    "ai_generated",
    "gated_to_lesson",
    "jp",
    "layer",
    "length_band",
    "level",
    "needs_review",
    "slug",
    "source_slugs",
    "title",
    "tokens",
    "translation",
    "uses"
  ]
}
