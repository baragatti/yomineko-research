{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://yomineko.dev/contracts/family.schema.json",
  "title": "family",
  "description": "A group of items that behave alike (a conjugation class, a particle set, a semantic field), with the rule that governs the group.",
  "x-yomineko": {
    "entity": "family",
    "records": 396,
    "packing": "list",
    "glob": "corpus/families/families.json",
    "stable_id_field": "slug",
    "generated_by": "scripts/contracts/build_schemas.py from contracts/_shapes.json",
    "vocabulary_policy": "Types, nesting and `required` are measured from the data. Value sets are not: every `enum` here is either declared in a design document, parsed from the code that produces the value, or curated in build_schemas.py with its source named in x-vocabulary. See contracts/README.md."
  },
  "type": "object",
  "properties": {
    "description": {
      "anyOf": [
        {
          "$ref": "common.schema.json#/$defs/LocaleText"
        },
        {
          "type": "null"
        }
      ],
      "description": "Authorable Layer-C prose. Empty on every record today; typed as the locale object it will hold rather than pinned to null."
    },
    "governing_rule": {
      "anyOf": [
        {
          "$ref": "common.schema.json#/$defs/LocaleText"
        },
        {
          "type": "null"
        }
      ]
    },
    "id": {
      "type": "integer",
      "minimum": 0
    },
    "importance_rank": {
      "type": "integer",
      "minimum": 0
    },
    "label": {
      "$ref": "common.schema.json#/$defs/LocaleText"
    },
    "members": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "integer",
            "minimum": 0
          },
          "intra_order": {
            "type": "integer",
            "minimum": 0
          },
          "is_core": {
            "type": "boolean"
          },
          "member_type": {
            "type": "string",
            "enum": [
              "grammar",
              "kanji",
              "vocab"
            ],
            "description": "Which registry the member id belongs to. Vocabulary owner: curated — scripts/ingest/migrations/001_init.sql (the `family_member.member_type` column comment). Widening it is an edit there, never a side effect of regenerating this schema from the data.",
            "x-vocabulary": {
              "owner": "curated",
              "source": "scripts/ingest/migrations/001_init.sql (the `family_member.member_type` column comment)"
            }
          },
          "note": {
            "anyOf": [
              {
                "$ref": "common.schema.json#/$defs/LocaleText"
              },
              {
                "type": "null"
              }
            ],
            "description": "Authorable Layer-C prose. Empty on every record today; typed as the locale object it will hold rather than pinned to null."
          },
          "ref": {
            "type": "string"
          },
          "slug": {
            "$ref": "common.schema.json#/$defs/StableId"
          }
        },
        "additionalProperties": false
      }
    },
    "slug": {
      "$ref": "common.schema.json#/$defs/StableId"
    },
    "spans_levels": {
      "type": "array",
      "items": {
        "$ref": "common.schema.json#/$defs/Level"
      }
    },
    "type": {
      "type": "string",
      "enum": [
        "conjugation_class",
        "contrast_pair",
        "function_set",
        "kanji_component",
        "particle_set",
        "phonetic_series",
        "semantic_field",
        "word_family"
      ],
      "description": "What kind of grouping this family is. Vocabulary owner: curated — scripts/ingest/migrations/001_init.sql (the `family.type` column comment). Widening it is an edit there, never a side effect of regenerating this schema from the data.",
      "x-vocabulary": {
        "owner": "curated",
        "source": "scripts/ingest/migrations/001_init.sql (the `family.type` column comment)"
      }
    }
  },
  "additionalProperties": false,
  "required": [
    "governing_rule",
    "id",
    "importance_rank",
    "label",
    "members",
    "slug",
    "spans_levels",
    "type"
  ]
}
