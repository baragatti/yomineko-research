{
  "work_file": "_README.md",
  "rows": [],
  "authoring_note": "No rows authored: the batch argument for this agent resolved to _README.md, which is this directory's documentation and contains zero passages (no `passages` array, no `questions_required`). The 286 reading_comp passages that need questions live in work-01.json .. work-12.json. This file is intentionally empty and keyed-row-compatible so that any assembler globbing authored-*.json contributes nothing from it and produces no duplicate (passage, question_index) keys."
}
